# Environment variables for local development of the web app
# Generate a secure random value for JWT_SECRET. Example (PowerShell):
# [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 } | ForEach-Object {[byte]$_}))

JWT_SECRET=

# NODE_ENV=development
